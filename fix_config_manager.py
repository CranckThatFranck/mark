"""Legacy helper kept only as historical reference.

This repository no longer uses one-off patch scripts to mutate source files in
place. Configuration bootstrapping now lives directly in `src/backend`.
"""

raise SystemExit("Legacy helper script. No action required.")
